<% content_for :container_width, "max-w-7xl" %>

<%= turbo_stream_from "tito_sync" %>

<%= render "sync_status", status: @sync_status %>

<%= form_with url: admin_users_path, method: :get, local: true, class: "mb-6 flex gap-2 flex-wrap" do %>
  <%= text_field_tag :q, @query,
        placeholder: "Search by name or email…",
        class: "input flex-1" %>
  <%= hidden_field_tag :sort, @sort %>
  <%= hidden_field_tag :dir, @dir %>
  <%= submit_tag "Search", class: "btn btn-navy" %>
  <% if @query.present? %>
    <%= link_to "Clear", admin_users_path(sort: @sort, dir: @dir), class: "btn btn-ghost" %>
  <% end %>
<% end %>

<% if @users.empty? %>
  <div class="card text-muted">
    <% if @query.present? %>
      No matches for "<%= @query %>".
    <% else %>
      No users yet.
    <% end %>
  </div>
<% else %>
  <p class="text-gray text-sm mb-3">
    RSVPs do not include talks, reception, and volunteer spots.
  </p>
  <div class="table-scroll">
  <table class="table">
    <thead>
      <tr>
        <th><%= sort_link("Attendee", "name") %></th>
        <th><%= sort_link("Role", "role") %></th>
        <th><%= sort_link("Last Seen", "last_seen") %></th>
        <th>Tito</th>
        <th><%= sort_link("RSVPs", "rsvps") %></th>
        <th><%= sort_link("# of volunteer spots", "volunteer_spots") %></th>
        <th><%= sort_link("# of activities hosting", "hosting") %></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td>
            <div class="font-medium text-navy"><%= user.full_name %></div>
            <div class="text-gray text-sm"><%= user.email %></div>
          </td>
          <td>
            <span class="badge badge-<%= user.role %>"><%= user.role.humanize %></span>
          </td>
          <td>
            <% if user.last_seen_at %>
              <%= user.last_seen_at.strftime("%b %-d · %-l:%M %p") %>
            <% else %>
              <span class="text-muted">Never</span>
            <% end %>
          </td>
          <td>
            <% if user.admin_ticket_url %>
              <%= link_to "View ticket →", user.admin_ticket_url,
                    target: "_blank", rel: "noopener noreferrer",
                    class: "text-blue underline" %>
            <% else %>
              <span class="text-gray text-sm">—</span>
            <% end %>
          </td>
          <td><%= @rsvp_counts[user.id] || 0 %></td>
          <td>
            <% if user.admin? || user.volunteer? %>
              <%= @volunteer_counts[user.id] || 0 %>
            <% else %>
              <span class="text-muted">—</span>
            <% end %>
          </td>
          <td><%= @hosting_counts[user.full_name] || 0 %></td>
          <td class="text-right">
            <div class="flex gap-2 justify-end">
              <%= link_to "View", admin_user_path(user), class: "btn btn-muted btn-small" %>
              <%= link_to "Edit", edit_admin_user_path(user), class: "btn btn-muted btn-small" %>
              <%= button_to "Delete", admin_user_path(user),
                    method: :delete,
                    data: { turbo_confirm: "Remove #{user.full_name}?" },
                    class: "btn btn-red btn-small" %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
<% end %>
